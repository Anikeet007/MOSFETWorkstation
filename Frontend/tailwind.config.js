/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This scans all your React components
  ],
  theme: {
    extend: {
      // This is where you can add custom colors like 'mosfet-blue' later
    },
  },
  plugins: [],
}